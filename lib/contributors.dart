import 'package:flutter/material.dart';

class Contributor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Hacktober Fest - Contributor"),
      ),
      body: Center(
        child: Text("Contributor List wil be shown here..!"),
      ),
    );
  }
}
